#ifndef F_FS_SSAO_MAP_GLS
#define F_FS_SSAO_MAP_GLS

#include "sh_uniform_locations.gls"

#ifndef ENABLE_SSAO
	#define ENABLE_SSAO 1
#endif
#if ENABLE_SSAO == 1
	layout(LAYOUT_ID(DESCRIPTOR_SET_RENDERER,DESCRIPTOR_SET_RENDERER_BINDING_SSAO_MAP)) uniform sampler2D u_ssao;
#endif

#endif
