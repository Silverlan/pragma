#include "stdafx_shared.h"
#include "pragma/lua/classes/llistener.h"
/*
DLLNETWORK void Lua_Listener_SetGain(lua_State *l,std::shared_ptr<ListenerHandle> &hL,float gain)
{
	ListenerHandle *listener = _lua_Listener_check(l,1);
	listener->GetEntity<Listener>()->SetGain(gain);
}

DLLNETWORK void Lua_Listener_GetGain(lua_State *l,std::shared_ptr<ListenerHandle> &hL)
{
	ListenerHandle *listener = _lua_Listener_check(l,1);
	float gain = listener->GetEntity<Listener>()->GetGain();
	Lua::PushNumber(l,gain);
}
*/