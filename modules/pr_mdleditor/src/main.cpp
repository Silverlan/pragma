#include "stdafx_mdleditor.h"
#include "mdl.h"
#include "vtx.h"
#include "vvd.h"
#include "phy.h"
#include "ani.h"
#include <iostream>
/*
int main(int argc,char *argv[])
{
	Model *mdl = nullptr;

	auto f = FileManager::OpenFile("antlion_guard.mdl","rb");
	import::MdlInfo mdlInfo(*mdl);
	//auto r = import::load_mdl(f,false,mdlInfo);

	//bool load_vvd(const std::string &name,std::vector<Vertex> &verts,std::vector<VertexWeight> &vertWeights);
	std::vector<Vertex> verts;
	std::vector<VertexWeight> vertWeights;
	//auto r = import::mdl::vvd::load_vvd("antlion_guard.vvd",verts,vertWeights);

	//std::vector<import::mdl::phy::PhyCollisionData> collisionObjects {};
	//auto r = import::mdl::phy::load_phy("antlion_guard.phy",collisionObjects);
	//std::cout<<"Success: "<<r<<std::endl;

	//import::mdl::ani::load_ani("antlion_guard.ani");

	//import::mdl::vvd::load_vvd("bicycle01a.vvd",verts,vertWeights);

	//std::vector<import::mdl::vtx::VtxBodyPart> bodyParts;
	//import::mdl::load_vtx("bicycle01a.dx90.vtx",bodyParts);


	//std::vector<Vector3> colVerts;
	//import::mdl::phy::load_phy("bicycle01a.phy",colVerts);

	//std::vector<std::string> textures;
	//import::load_mdl(nullptr,"bicycle01a.mdl",*mdl,false,textures);

	//std::vector<Vertex> verts;
	//
	//
	for(;;);
	return 0;
}
*/