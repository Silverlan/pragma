#ifndef F_CS_WATER_SURFACE_INFO_GLS
#define F_CS_WATER_SURFACE_INFO_GLS

layout(std140,set = SHADER_STORAGE_WATER_SURFACE_INFO_SET,binding = SHADER_STORAGE_WATER_SURFACE_INFO_BINDING) uniform CS_SURFACE_INFO
{
	vec3 origin;
	float stiffness;
	float propagation;
	float maxHeight;
	uint spacing;
	uint width;
	uint height; // Actually 'length', but 'length' is a reserved name
} u_surfaceInfo;

#endif
