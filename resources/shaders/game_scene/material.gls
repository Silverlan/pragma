#ifndef F_MATERIAL_GLS
#define F_MATERIAL_GLS

struct MaterialInfo
{
	uint albedoTextureArrayIndex;
	uint normalTextureArrayIndex;
	uint ambientOcclusionTextureArrayIndex;
	uint metallicTextureArrayIndex;
	uint roughnessTextureArrayIndex;

	uint placeholder0;
	uint placeholder1;
	uint placeholder2;
};

#endif
