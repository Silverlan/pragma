#ifndef F_SH_VIS_LIGHT_BUFFER_GLS
#define F_SH_VIS_LIGHT_BUFFER_GLS

#include "../modules/sh_uniform_locations.gls"

struct VisibleIndex {
	int index;
};
layout(std430,LAYOUT_ID(SHADER_UNIFORM_TILE_VIS_LIGHT_INDEX_BUFFER_SET,SHADER_UNIFORM_TILE_VIS_LIGHT_INDEX_BUFFER_BINDING)) buffer VisibleLightTileIndicesBuffer {
	VisibleIndex data[];
} visibleLightTileIndicesBuffer;

#endif
