#ifndef F_SH_GLOW_GLS
#define F_SH_GLOW_GLS

#define SHADER_VERTEX_DATA_LOCATION 0

#include "../modules/sh_export.gls"
#include "../modules/sh_uniform_locations.gls"

layout(location = 4) EXPORT_VS VS_OUT
{
	vec2 vert_uv;
}
#ifdef GLS_FRAGMENT_SHADER
	fs_in
#else
	vs_out
#endif
;

#endif
